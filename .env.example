# TextToVec 服务地址
TEXTTOVEC_BASE_URL=http://localhost:8099
TEXTTOVEC_DIMENSION=128

# ES 配置
# ES_MOCK=true  时使用内存 mock，无需真实 ES 服务
# ES_MOCK=false 时连接真实 ES，需同时配置以下项
ES_MOCK=true
ES_URL=http://localhost:9200
ES_USERNAME=
ES_PASSWORD=
ES_VERIFY_CERTS=true

# 日志目录（支持相对路径和绝对路径）
LOG_DIR=logs
